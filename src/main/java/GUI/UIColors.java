package GUI;

import java.awt.*;

/**
 * Centralized color palette for the shopping app.
 * Modern teal/blue theme with consistent primary, accent, and surface colors.
 */
public class UIColors {
    // Primary color - used for main actions, highlights, buttons
    public static final Color PRIMARY = new Color(0x0B74D1);
    
    // Accent color - used for secondary actions, hover states
    public static final Color ACCENT = new Color(0x00BFA6);
    
    // Surface color - used for backgrounds, cards
    public static final Color SURFACE = new Color(0xFFFFFF);
    
    // Muted color - used for borders, disabled elements
    public static final Color MUTED = new Color(0xF4F7FA);
    
    // Danger color - used for destructive actions, errors, badges
    public static final Color DANGER = new Color(0xE53935);
    
    // Additional colors for convenience
    public static final Color TEXT_PRIMARY = new Color(0x1F1F1F);
    public static final Color TEXT_SECONDARY = new Color(0x757575);
    public static final Color BORDER = new Color(0xE0E0E0);
    public static final Color SHADOW = new Color(0x000000);
    
    // Navbar background
    public static final Color NAVBAR_BG = PRIMARY;
    
    // Hover states
    public static final Color HOVER_LIGHT = new Color(0xE3F2FD); // light blue
}
